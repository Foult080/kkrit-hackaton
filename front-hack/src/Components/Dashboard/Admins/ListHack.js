import React from "react";
import PropTypes from "prop-types";

const ListHack = (props) => {
  return (
    <div>
      <h1>Hello from ListHack</h1>
    </div>
  );
};

ListHack.propTypes = {};

export default ListHack;
